.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.hypothesis/
.ruff_cache/
*.egg-info/
dist/
build/

# Generated artefacts — rebuilt by `make data`
data/interim/
data/processed/
site/

# The weather cache IS committed on purpose: reproducibility depends on the same
# seed producing the same weather. Do not add data/cache/ here.

# built site — regenerated by scripts/build_site.py in CI
site/

# built wheel — CI rebuilds it; never commit a stale copy
dist/
